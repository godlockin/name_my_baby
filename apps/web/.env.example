# Gemini API Key (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here

# Environment
NODE_ENV=development

# Cloudflare D1 (auto-configured via wrangler)
# DB=<database_binding>

# Cloudflare R2 (auto-configured via wrangler)
# BUCKET=<bucket_binding>
